using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Degiskenler : MonoBehaviour
{
    /* Start is called before the first frame update
    int
    int cansayisi = -4; /tam sayý deðiþkenler
    float
    float ates = 38.9f; /virgüllü sayý deðiþkeneri
    string
    string oyuncu_adi = "suleyman"; /metinsel ifadeler
    string tc_no = "1122334455667";
    bool
    bool canli_mi = true; /false / doðru veya yanlýþ ifade döndüren ifadeler
    */
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
